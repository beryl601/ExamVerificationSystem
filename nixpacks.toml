[phases.setup]
aptPkgs = ["libx11-6", "libx11-dev", "libgl1", "libglib2.0-0", "libsm6", "libxext6", "libxrender-dev", "cmake", "build-essential", "python3-dev"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install --no-binary dlib dlib",
  "pip install -r requirements.txt"
]